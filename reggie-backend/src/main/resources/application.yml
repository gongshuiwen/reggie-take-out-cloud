mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: ASSIGN_ID
spring:
  main:
    allow-circular-references: true
    allow-bean-definition-overriding: true
springdoc:
  api-docs:
    path: /v3/api-docs
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: org.example.reggie
  swagger-ui:
    enabled: false
knife4j:
  enable: true
  setting:
    language: zh_cn
